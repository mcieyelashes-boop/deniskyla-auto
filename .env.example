# Server-side (Vercel env var — NOT prefixed with VITE_)
ANTHROPIC_API_KEY=your_api_key_here

# Client-side flag for local dev (tells the app the proxy is available)
VITE_ANTHROPIC_API_KEY=your_api_key_here
